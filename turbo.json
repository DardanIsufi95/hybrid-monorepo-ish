{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"outputs": []
		},
		"docker-build": {
			"dependsOn": ["build"],
			"outputs": []
		}
	}
}
